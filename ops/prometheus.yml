global:
  scrape_interval: 10s

scrape_configs:
  - job_name: gateway
    static_configs: [{ targets: ["gateway:8080"] }]
  - job_name: api
    static_configs: [{ targets: ["api:8081"] }]
  - job_name: indexer
    static_configs: [{ targets: ["indexer:9100"] }]
