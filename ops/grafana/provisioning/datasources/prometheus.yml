apiVersion: 1
datasources:
  - name: Prometheus
    uid: PROM              # the dashboard panels reference this by uid
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
  - name: Loki
    uid: LOKI
    type: loki
    access: proxy
    url: http://loki:3100
